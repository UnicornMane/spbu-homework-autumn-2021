#include <iostream>

int main()
{
    int h, m, s;

    std::cin >> h >> m >> s;
    int sum1 = 3600 * h + 60 * m + s;

    std::cin >> h >> m >> s;
    int sum2 = 3600 * h + 60 * m + s;

    std::cout << sum2 - sum1;

    return EXIT_SUCCESS;
}
