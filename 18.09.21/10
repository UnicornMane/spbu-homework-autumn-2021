#include <iostream>

int main()
{
    int f, s;
    std::cin >> f >> s;
    bool sign = signbit((long double)(f - s));
    int tmp = -1 * (int)sign;
    std::cout << (f + s + tmp * (f - s)) / 2;
    return EXIT_SUCCESS;
}
