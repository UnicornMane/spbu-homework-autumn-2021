#include <iostream>

int main()
{
    int a = 0;
    int b = 0;
    std::cin >> a >> b;
    a = a + b;
    b = a - 2 * b;
    b = (a + b) / 2;
    a -= b;
    std::cout << a << " " << b << std::endl;

    return EXIT_SUCCESS;
}
